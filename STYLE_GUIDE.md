# 보미 건강노트 — 카드뉴스 스타일 가이드 & SEO 가이드

`api/cardnews.js` 파이프라인(주제 선정 → LLM 카피 → 표지 사진 → 카드뉴스 렌더 →
텔레그램 승인 → 인스타그램 발행)이 실제로 따르는 규칙을 문서화한 것입니다. 코드를
고칠 때(특히 `lib/cardnewsRender.mjs`의 템플릿, `api/cardnews.js`의 `COPY_SYSTEM_PROMPT`)
이 문서와 어긋나지 않게 함께 갱신하세요.

이 가이드는 국내 건강/의학 인스타그램 채널(코메디닷컴 `@kormedi_`, 하이닥
`@hidoc_health_news` 등 팔로워 규모가 있는 건강 전문 매체 계정)의 카드뉴스 포맷
관찰과, 2026년 기준 Instagram 카루셀 알고리즘/SEO 공개 자료(출처는 문서 하단
참고)를 바탕으로 정리했습니다.

## 1. 톤 앤 매너 (보미 브랜드 원칙)

- **독자**: 60대 이상 시니어 또는 그 가족. 존댓말, 쉬운 말, 짧은 문장.
- **자극적 표현 금지**: "당장 안 하면 큰일" 류의 공포 마케팅·클릭베이트 금지.
  담백하고 신뢰가는 톤을 유지합니다(보미 앱 전체의 "몰입을 방해하지 않는다"
  원칙과 동일선상 — PROJECT_STATUS.md 참고).
- **단정 금지**: 의학적 진단·처방처럼 단정하지 않고, 실천 가능한 정보 + 따뜻한
  시선의 마무리로 끝냅니다. 이 원칙이 마지막 슬라이드("오늘의 건강 고찰")의
  존재 이유입니다.
- **면책 문구**: 모든 카드뉴스 마지막 슬라이드에 "정보 제공 목적, 진단·치료
  대체 아님, 증상 있으면 의료진 상담" 문구를 항상 포함합니다.

## 2. 비주얼 시스템

`lib/cardnewsRender.mjs`에 이미 구현된 값 — 코드가 최종 소스이고 아래는 그 요약.

| 항목 | 값 |
|---|---|
| 캔버스 크기 | 1080×1350px (4:5 세로형 — 피드에서 가장 넓은 영역 차지, 카루셀 표준) |
| 폰트 | Pretendard (Regular/Bold/ExtraBold) — 보미 앱과 동일 타이포 아이덴티티 |
| 배경 | `#FBF8F3`(기본), `#EDEAE4`(고찰 슬라이드 — accent-soft) |
| 포인트 색 | `#9C7A54`(accent), `#6F5636`(navy) — 보미 앱과 동일한 웜 베이지 팔레트 |
| 본문 텍스트 | `#3A3128`(ink), `#7A6F60`(sub) |
| 슬라이드 구성 | 표지(사진 배경 가능) → 본문 3장(번호 배지+헤딩+1~2문장) → 고찰(마무리+면책) = 총 5장 |
| 표지 사진 | OpenAI 이미지 생성(gpt-image-1) 자동 생성, 실패 시 타이포그래피 전용 표지로 폴백 |

**슬라이드 수를 5장으로 고정한 이유**: 외부 자료 기준 5~7장 카루셀이 정적 이미지
대비 저장 3.4배·공유 2.1배 더 나온다는 데이터가 있고, 시니어 독자에게는 이보다
길면 피로도가 높아지므로 하한선(5장)에 맞췄습니다.

## 3. 슬라이드별 카피 규칙 (LLM 프롬프트 기준, `api/cardnews.js`)

1. **표지**: 헤드라인 18자 내외(첫 슬라이드는 40자 미만이 가독성에 좋다는 카루셀
   SEO 가이드 기준보다 더 짧게 — 시니어 가독성 우선), 궁금증을 유발하되 자극적이지
   않은 후킹 문장. 카테고리 라벨(kicker) 8자 내외.
2. **본문 3장**: 헤딩 12자 내외 + 본문 1~2문장. 슬라이드 하나에 메시지 하나만
   (여러 정보를 한 슬라이드에 욱여넣지 않기).
3. **고찰(마지막 슬라이드)**: 2~3문장, 따뜻하고 담백한 마무리. 저장을 유도하는
   한 줄을 캡션 쪽에서 다루고(아래 SEO 섹션), 슬라이드 자체는 광고 느낌 없이
   진짜 "생각할 거리"로 끝냅니다.

## 4. SEO / 발견성 가이드

Instagram 카루셀 알고리즘(2026년 기준 공개 자료, 출처 하단)이 실제로 보는 신호와,
그에 맞춰 이 파이프라인이 하는 것/안 하는 것:

- **스와이프 = 개별 참여 신호**: 슬라이드를 넘기는 행위 자체가 "저장/공유"와
  비슷한 무게의 신호로 재노출(24~48시간 후 2차 노출)에 영향을 줍니다. → 그래서
  슬라이드 1장에 메시지 1개만 담아 "다음 장이 궁금하게" 만드는 게 슬라이드 수를
  늘리는 것보다 중요합니다.
- **완독률(completion rate)이 핵심 지표**: 슬라이드 수가 너무 많으면 완독률이
  떨어져 역효과 → 5장으로 제한한 이유이기도 함.
- **표지(1번 슬라이드)가 썸네일**: 고대비 + 굵은 타이포 + 여백. 이 파이프라인은
  사진 배경 + 하단 그라디언트 + 흰 텍스트 조합으로 대비를 확보합니다.
- **마지막 슬라이드의 CTA**: 저장/공유를 유도하는 문구가 도움이 된다는 게 공통된
  가이드입니다. `COPY_SYSTEM_PROMPT`가 캡션에 "저장해두고 필요할 때 다시 보세요"
  류의 저장 유도 문장을 넣도록 지시합니다(슬라이드 자체가 아니라 캡션에 — 슬라이드는
  광고 느낌을 피하기 위해 순수하게 유지).
- **해시태그**: 5~8개, 다음 3그룹을 섞습니다 — 넓은 태그(#건강, #건강정보),
  좁은 태그(#시니어건강, #혈압관리 등 그날 주제에 맞는 것), 브랜드 태그
  (#보미건강노트). 30개까지 채우는 과거 방식은 지양(Instagram 공식 권장도
  소수의 관련성 높은 태그 쪽으로 이동한 지 오래).
- **캡션 구조**: 첫 줄에 핵심 후킹(피드에서 "더 보기" 접히기 전 노출되는 부분),
  이후 2~3문장 설명, 마지막에 저장 유도 + 해시태그.
- **대체 텍스트(alt text)**: Graph API의 콘텐츠 발행 엔드포인트로는 대체 텍스트를
  안정적으로 설정할 수 있다는 공식 문서를 확인하지 못했습니다 — 필요하면 발행 후
  Instagram 앱에서 게시물 수정 화면에서 수동으로 추가하는 걸 권장합니다(자동화
  범위 밖으로 명시적으로 남겨둠).

## 5. 운영 규칙

- **중복 방지**: 최근 14일 내 이미 초안으로 만든 원문 링크는 후보에서 제외
  (`lib/supabaseAdmin.mjs`의 `listRecentCardnewsSourceUrls`).
- **소스**: 코메디닷컴(`kormedi.com/feed/`, 1차) + 메디컬투데이(`mdtoday.co.kr`,
  건강 키워드 필터링 후 보조) — 선정 근거는 `lib/healthNewsFeed.mjs` 상단 주석 참고.
- **게시 주기**: 하루 1회 (Vercel Cron, 매일 08:00 KST) — 과다 게시는 계정
  신뢰도에 오히려 해가 될 수 있어 의도적으로 1일 1건만 유지합니다.

## 참고 자료

- [Instagram Carousel Algorithm 2026 Guide](https://www.trymypost.com/blog/instagram-carousel-algorithm-2026-guide)
- [Instagram Carousel Best Practices 2026 (Metricool)](https://metricool.com/instagram-carousels/)
- [Instagram Content Publishing — Meta for Developers](https://developers.facebook.com/docs/instagram-platform/content-publishing/)
- 하이닥(`@hidoc_health_news`), 코메디닷컴(`@kormedi_`) — 국내 건강 전문 매체
  인스타그램 채널 포맷 관찰
