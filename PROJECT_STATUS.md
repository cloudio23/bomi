# 보미 AI 프로젝트 현황

이 문서는 `bomi-team-lead` 에이전트가 진행 상황 검토·계획 제안 전에 항상 먼저 읽고,
매 라운드 작업 후 갱신하는 상태 기록입니다.

_최종 갱신: 2026-07-30_

## 운영 참고 (중요)

- **API 키는 절대 채팅으로 붙여넣지 말고 Vercel 환경변수(Settings → Environment
  Variables)에 직접 입력**하세요. 한 번 채팅에 붙여넣은 키는 유출된 것으로 간주하고
  즉시 재발급하는 걸 권장합니다.
- **`.claude/agents/`의 커스텀 서브에이전트(bomi-creative-director/bomi-qa/bomi-team-lead)는
  이 환경의 Task 도구로 이름 지정해서 실행이 안 됩니다** (`Agent type 'bomi-creative-director'
  not found` 오류 확인함, 등록된 건 claude/claude-code-guide/Explore/general-purpose/Plan/
  statusline-setup뿐). 그래서 실제로는 메인 어시스턴트가 각 에이전트 정의서(.md)의 기준을
  직접 적용해서 일하는 방식으로 운영 중입니다 — 이 방식이 실질적으로 동일한 결과를 내므로
  문제는 없지만, "그 에이전트한테 물어봐줘"라고 하셔도 실제로는 제가 그 역할을 흉내내는
  것이라는 점을 알아두시면 좋겠습니다.

## 이번 라운드에서 결정된 것 (창업자 확인 완료)

- **AI 백엔드는 하이브리드**: 기본값 **Gemini 무료 티어**, 무료 티어로 부족해지면
  `AI_PROVIDER=anthropic` 환경변수로 **수동 전환**. 자동 폴백은 하지 않기로 결정함
  (Gemini가 일시적으로 실패해도 Claude로 조용히 넘어가서 예상 못한 과금이 생기는 걸
  방지하기 위함). → `lib/aiProviders.mjs`로 구현 완료, `api/chat.js`와 `qa/` 양쪽이 이걸
  공유해서 씀.
- **AI Enhancement (①②③④ 진행, ⑤ 보류)**: 저비용 모델이 짧고 정확하게 답하도록 주제별
  응답 포맷(검색/지도/사주/경제/말벗/부동산정책/정치뉴스)을 `systemPrompt()`에 규정하고,
  정치·부동산·투자 관련 중립성 가드레일을 추가함. 또한 매 응답 끝에 모델이
  `[[선택지: A | B | C]]` 마커로 예상 사용자 발화 후보를 붙이도록 지시하고,
  `index.html`이 이를 파싱해 탭 가능한 선택지 칩으로 렌더링하도록 구현함(입력 부담 감소).
  실시간 데이터가 필요한 지도/경제 정확도 개선(⑤)은 공공 API 연동 로드맵과 함께 보류.

## 크리에이티브 디렉터 리뷰 결과 (index.html 전체 진단)

"투박하다"는 창업자 피드백을 6가지로 구체화함, 영향도 순:
1. ✅ **완료** — 설치된 PWA(`display:standalone`)에서도 목업 카드(둥근 모서리+그림자+여백)로
   남아 실제 기기 화면을 안 채우던 문제. 가장 근본적인 원인으로 지목되어 최우선 수정.
2. **미해결** — `font-family:"Pretendard"`만 선언돼 있고 실제로 로드하는 `@font-face`/CDN이
   없어서 매 기기 시스템 기본 글꼴로 렌더링됨. 타이포 정체성 부재.
3. **미해결** — 오로라 보더(`.auroraBorder`)가 앱 고유 색 토큰을 안 쓰고 5개의 새 임의
   색상(`#6C63FF` 등)을 도입함.
4. **미해결** — `#EEF2FF`+navy 조합이 6곳 이상에서 반복되는데 `--accent-soft` 같은 역할
   변수로 토큰화가 안 됨.
5. **미해결** — 그림자(depth)가 `.app` 바깥 프레임에만 있고 내부 카드들은 전부 flat+border.
6. **미해결** — 타입 스케일이 11~23px 사이 10단계로 흩어져 있고 뚜렷한 배율이 없음.
7. **미해결** — 스플래시 화면 자체가 없음 (로딩을 가릴 필요는 없지만 브랜드 모먼트로 제안됨).

## 완료됨

- **① 설치된 PWA edge-to-edge 수정** (index.html): `@media (display-mode: standalone)`
  블록을 스타일시트 맨 끝에 추가 — `.app`의 둥근 모서리/그림자/테두리를 0으로, 높이를
  100dvh로, `--app-radius` CSS 변수로 `.cameraOverlay`/`.auroraBorder`도 함께 정사각형화.
  iOS 노치 대응으로 `viewport-fit=cover` + `env(safe-area-inset-*)` 패딩도 추가.
  **브라우저 도구 없어 실기기 확인 아직 안 됨** (F 항목과 동일 제약).
- **실기기 사용 후 창업자 직접 피드백 4건 반영** (index.html): (1) 폰에서 페이지 전체가
  위아래로 스크롤되던 문제 — `html,body{height:100%}` + `body{overflow:hidden}`으로 바깥
  페이지는 절대 안 스크롤되고 내부 `.chat`/`.scroll`만 스크롤되도록 수정. (2) 헤더가 진한
  파란 블록으로 분리돼 보이던 것을 `.chat` 그라디언트 시작색(`#F7F9FF`)과 맞춰 대화창의
  연장처럼 이어지게 수정, 아바타만 포인트 컬러로 유지. (3) `.quickchips` 배경을 흰색에서
  `#EFF3FB`(`.chat` 그라디언트 끝색)로 맞춤. (4) 체크리스트 접기/펼치기 화살표를
  12px 회색 글자에서 24px 원형 배경 버튼으로 키워서 탭 가능한 UI임을 명확히 함. 헤더 우측
  버튼도 햄버거(☰)에서 톱니바퀴(⚙️)로 바꿔 "설정" 의미를 명확히 함.
  이번에도 브라우저 도구 없어 실기기 확인은 못 함.

- **2차 실기기 피드백 반영** (index.html): (1) `.pinned-checklist` 배경도 흰색→`#F7F9FF`로
  통일 (지난 라운드에 남겨뒀던 흰 띠 문제 해소). (2) 헤더를 대폭 단순화 — 아바타/부제 다
  없애고 "보미"만 큰 글씨로 중앙 고정(`position:absolute`로 버튼 유무와 무관하게 항상 정중앙),
  우측엔 설정 버튼 하나만. 아이콘도 톱니바퀴(⚙️)에서 원래 선호하시던 삼선(☰)으로 되돌림 —
  지난 라운드에 제가 "설정 의미가 더 명확하다"고 임의로 바꿨던 판단은 창업자 취향과 안
  맞았음. `chatStatus` DOM/JS 참조도 함께 정리(죽은 코드 안 남김). (3) **"사용에 불편감은
  없으신가요?" 채팅 인터럽트 메시지를 완전히 제거** — 몰입을 깬다는 피드백. `maybeAskFeedback`/
  `pendingFeedback`/관련 상수를 전부 삭제. 단, 프로필 화면의 "접수된 피드백" 조회 UI와
  `saveFeedback`/`renderFeedbackList`는 과거 데이터 열람용으로 남겨둠(이제 새로 쌓이진 않음).
  (4) **응답 길이 정책 재조정** — 지난 AI Enhancement 라운드에서 넣은 "3문장 이내로 짧게"
  규칙이 실제로는 단답형으로 느껴진다는 피드백. `systemPrompt()`를 "단답 금지, 자연스러운
  대화체로 보통 3~5문장"으로 완화하고 (`index.html` + `qa/lib/systemPrompt.mjs` 동기화),
  `api/chat.js`/`lib/aiProviders.mjs`의 `maxTokens`도 300/400→500으로 상향. `qa/rubric.mjs`의
  `readability` 기준도 예전 "3문장 이내" 문구가 남아있던 걸 새 정책에 맞게 수정함(안 고쳤으면
  QA가 이제 정상적으로 길어진 답변을 오탐으로 FAIL 처리했을 것).

- **3차 실기기 피드백 반영 — 체크리스트 위치/아이콘 스타일** (index.html): (1) 체크리스트를
  더 이상 헤더 아래 항상 자리를 차지하는 바(bar)로 두지 않고, 헤더 좌측의 아이콘 버튼으로
  옮김(우측 설정 버튼과 대칭). `.pinned-checklist`를 `position:absolute` 드롭다운 패널로
  전환해서 평소엔 공간을 전혀 안 차지하고, 버튼을 누르면 대화창 위에 떠서 나타남 — `.chat`이
  자동으로 그 자리까지 확장됨. 체크리스트 진행률("1/3")은 버튼 위 작은 배지로 표시해서
  펼치지 않아도 한눈에 보이도록 유지. (2) 카메라/키보드/대화 독 버튼과 새 체크리스트
  버튼의 이모지 아이콘을 전부 손으로 그린 인라인 SVG 라인 아이콘(얇은 스트로크, 모노톤,
  `currentColor` 사용)으로 교체 — 외부 아이콘 폰트/CDN 없이 애플 SF Symbols류의 깔끔한
  느낌을 내기 위함. 설정 버튼(☰)은 이미 순수 글리프라 그대로 둠. 이번에도 브라우저 확인은
  못 함 — SVG 아이콘이 의도한 모양대로 보이는지 특히 직접 확인 필요.

- **카카오 카나나(kanana) 참고 리디자인** (index.html, 창업자가 스크린샷 4장 제공): 대대적인
  비주얼 개편.
  - **색상 전면 교체**: 파란색 테마 → 카나나처럼 거의 무채색 + 포인트색은 웜 베이지/브라운으로.
    `--blue`/`--navy` 변수 값 자체를 베이지 계열(`#9C7A54`/`#6F5636`)로 바꾸고, `--ink`/`--sub`/
    `--bg`/`--line`도 차가운 블루그레이에서 따뜻한 뉴트럴 톤으로 전부 교체. 하드코딩돼 있던
    파란빛 도는 옅은 배경/보더 hex(`#EEF2FF`, `#F7F9FF`, `#F1F3F8` 등 10여 개)도 전부 웜톤으로
    맞춤. 새 `--accent-soft` 변수를 추가해서 지난 크리에이티브 리뷰에서 지적했던 "`#EEF2FF`
    토큰화 안 됨" 문제(항목 K)도 같이 해결함. **참고**: 변수 이름 자체(`--blue`, `--navy`)는
    리스크·시간 관계상 안 바꾸고 값만 바꿨음 — 지금은 베이지색인데 이름이 "블루"라 헷갈릴 수
    있음, 나중에 `--accent`로 이름까지 바꾸는 건 작은 정리 작업으로 남겨둠.
  - **말풍선 없는 봇 메시지**: 카나나처럼 보미(어시스턴트)의 답변은 더 이상 테두리 있는
    말풍선이 아니라 맨 텍스트로 표시. 사용자 메시지만 말풍선 유지하되 색은 파란색→웜그레이
    (`#EDEAE4`)로 변경.
  - **AI 안내 문구 + 피드백 아이콘 4개(👍👎🔄⬆️)** 추가, 실제 작동함: 👍/👎는 기존에
    만들어뒀던 `saveFeedback()`(지난 라운드에 죽여뒀던 피드백 인터럽트 대신 조용히 기록만
    하는 용도로 부활시킴)에 저장, 🔄(재생성)은 같은 질문으로 `callBomi()` 다시 호출해서
    답변을 아래에 추가로 붙임(기존 답변 삭제는 안 함, 단순화), ⬆️(공유)는 `navigator.share`
    우선 시도 후 실패 시 클립보드 복사.
  - **예상 답변 선택지**를 알약 모양 칩 목록에서 점선 밑줄 텍스트 링크 목록으로 변경(카나나
    스타일).
  - 헤더 우측에 새로고침 아이콘은 추가하지 않기로 확인(설정 버튼만 유지).

- **카나나 리디자인 2차 피드백 반영** (index.html): (1) 링 아이콘(컬러 도넛 모양)을 봇
  메시지·"생각 중" 인디케이터 양쪽 다 제거 — 더 단순하게. (2) "생각 중" 표시를 링+텍스트에서
  텍스트 + **오로라처럼 일렁이는 그라디언트 바**(`.thinking-aurora`, 기존 팔레트 톤 `--blue`/
  `--navy`/`--accent-soft`로 `background-position` 애니메이션)로 교체 — 지난번 만든 은은한
  블러 배경 방식 대신 더 뚜렷한 시각 효과로 변경. (3) 피드백 아이콘 줄을 매 답변마다 보여주는
  대신, **답변 길이 80자 이상일 때만** 표시하도록 게이팅 — "어려운 작업일 때만 보여달라"는
  요청을 구현할 수 있는 명시적 신호가 없어서 글자 수를 근사치로 사용함 (`FEEDBACK_MIN_LENGTH`
  상수로 조정 가능, 실제 써보고 기준을 조정할 필요 있음). (4) 입력창 하단 전체를 카나나
  스타일로 리디자인 — `.inputbar` 배경을 흰 바에서 대화창과 같은 톤(`#F6F3EE`)으로 통일해서
  경계선을 없애고, 카메라/키보드/대화 독 버튼과 키보드 눌렀을 때 나오는 입력창(`.typebar`)을
  각각 흰 배경+그림자가 있는 "떠 있는" 카드/알약 모양으로 변경.
  문법 검사 통과. **브라우저 확인 불가** — 특히 오로라 시머 애니메이션 속도/색감, 플로팅
  버튼 그림자 톤, 80자 기준이 실제로 적절한지는 직접 써보고 조정 필요.

- **핀치줌/화면 드래그 방지**: viewport meta에 `maximum-scale=1.0, user-scalable=no` 추가,
  `body`에 `touch-action:none` + `overscroll-behavior:none` + `user-select:none`(+ iOS 롱프레스
  콜아웃/탭 하이라이트 제거)로 화면 자체가 핀치줌·바운스·드래그로 안 밀리게 함. 단,
  `.chat`/`.scroll`/`#onboardingView`(실제 스크롤이 필요한 영역)는 `touch-action:pan-y`로
  세로 스크롤만 허용, `input`/`textarea`/메시지 텍스트는 `user-select:text`로 선택·입력 유지.
  **참고**: `user-scalable=no`는 WCAG 접근성 권장사항(확대 허용)과 상충하는 부분이 있는데,
  이미 어르신 눈높이로 폰트 크기를 키워둔 상태라 실용적으로 허용 범위로 판단하고 요청대로
  적용함. `theme-color`도 예전 파란색(#1861FE)이 남아있던 걸 새 베이지 톤으로 맞춤(사소한
  누락 수정).

- **레이아웃 압축 + 타자기 효과** (index.html, 실기기 피드백): (1) 헤더 패딩을 16px→10px로
  줄여서 더 좁고 위로 붙게 함. (2) 카메라/키보드/대화 독 버튼에서 텍스트 라벨을 없애고
  (`aria-label`은 유지해 접근성은 안 깨짐) 52px 원형 아이콘 전용 버튼으로 압축, 독/입력창
  영역의 상하 여백도 줄여서 하단에 남는 빈 공간을 줄임. standalone 모드의 safe-area 여백
  기준값도 16px→10px로 같이 낮춤. (3) `.thinking-aurora` 바 두께를 7px→16px로 키움.
  (4) **타자기 효과**: 실시간 새 답변(`sendMessage`/`regenerateReply`)은 이제 전체가 한 번에
  나타나지 않고 `typewriterReveal()`로 2글자씩 순차적으로 채워짐(진짜 스트리밍이 아니라
  이미 다 받은 응답을 화면에서 흉내 내는 것). 안내 문구·피드백 아이콘은 타이핑이 끝난 뒤에
  나타나도록 함. 과거 대화 다시 불러오기(새로고침 시)와 온보딩 인트로 메시지는 타자기 효과
  없이 그대로 즉시 표시(반복 재생 방지).
  문법 검사 통과. **브라우저 확인 불가** — 특히 타자기 속도(2글자/16ms)와 압축된 여백이
  실제로 알맞은지 확인 필요.

- **3차 미세조정 — 하단 여백 더 키우고 타자기 속도 늦춤** (실기기 피드백): 지난 라운드의
  압축이 부족했음. (1) 독/입력창 버튼 아래 여백을 10px→22px로 키우고 `.inputbar`
  `min-height`도 62px→82px로 재계산(버튼이 실제로 여백 안에 딱 맞게, 넘치지 않도록)해서
  버튼이 화면 하단에서 눈에 띄게 더 위로 떠 보이게 함. standalone 모드 하단 safe-area
  기준값도 10px→14px로 추가 상향. (2) 타자기 속도를 2글자/16ms→2글자/26ms로 늦춤
  (약 1.6배 느리게, "조금만 느리게"라는 요청에 맞춰 과하지 않게 조정).
  문법 검사 통과. **브라우저 확인 불가** — 이번에도 실제로 보고 조정 필요할 수 있음.

- **프로덕션 버그 수정**: `systemPrompt()`에 오늘의 체크리스트 완료 상태가 전혀 전달되지
  않아 슬라이드 7의 "치매 리마인드" 데모가 실제로는 작동하지 않던 문제를 고쳤음. 인사말이
  매일 다양해지도록 규칙도 추가함.
- **QA 하네스** (`qa/`): 어르신 페르소나 5명, 시나리오 7종, 채점 기준 6종 + "인사 다양성"
  기준 구축. `QA_MOCK=1`로 배선까지 확인 완료. `lib/aiProviders.mjs` 공유 모듈을 쓰도록
  리팩터링 완료 (더 이상 Anthropic 전용 코드 중복 없음). **실제 API로는 아직 실행한 적
  없음** (이제 Gemini 무료 티어라 실행 비용 자체는 거의 안 듦).
- **크리에이티브 입력창 리디자인** (`index.html`): 카메라/키보드/대화 3버튼 독, 키보드
  버튼 누르면 입력창으로 확장되는 크로스페이드, 카메라 버튼 누르면 검은 화면이 버튼 위치에서
  퍼지는 아이리스 전환(플레이스홀더 카메라 화면), 대화 버튼 누르면 화면 테두리 오로라 글로우
  프리뷰. JS/CSS 문법 검사만 통과, **브라우저에서 실제로 본 적은 없음** (이 환경에 브라우저
  도구 없음).
- **서브에이전트 3종**: `bomi-creative-director`(디자인/모션), `bomi-qa`(QA 하네스 운용 +
  AI Enhancement/프롬프트 효율화), `bomi-team-lead`(현황 검토·계획 승인·사업/비용 자문,
  지금 이 문서를 관리하는 주체).

## 검증 대기 (완료됐지만 확인 안 됨)

- 새 입력창 애니메이션이 실제 화면에서 의도한 느낌으로 보이는지 — 브라우저로 직접 확인 필요.
- QA 하네스를 실제 `ANTHROPIC_API_KEY`로 돌려서 진짜 점수를 받아본 적 없음 (전체 실행 시
  약 90~100회 API 호출 비용 발생).
- 카나나 스타일 리디자인(색상 전면 교체, 말풍선 없는 봇 메시지, 피드백 아이콘, 링 아이콘,
  "생각 중" 그라디언트) 전체 — 브라우저로 실제 확인 필요. 특히 웜 베이지 색상 대비와 링
  아이콘 모양이 의도대로 나오는지.

## 알려진 갭 (발견했지만 아직 고치지 않음)

1. ~~피드백 질문/응답이 완전히 하드코딩됨~~ → **해결(제거)**: 슬라이드 11의 그 기능 자체를
   "몰입을 방해한다"는 이유로 완전히 뺐음 (다양화가 아니라 제거로 결론).
2. **"매일 아침 먼저 말 거는" 선제적 오프닝이 실제로는 최초 가입 시 1회만 실행**됨. 매일
   서버가 먼저 말을 거는 크론/푸시 메커니즘은 없음.
3. **기기 간 동기화 없음** (localStorage 전용) — `README.md`에 이미 명시된 기존 한계.
4. **실제 카메라 촬영·비전 AI 미구현** — 지금은 UI 전환 애니메이션만 있고 "준비 중" 화면.
5. **음성 대화(제미나이 라이브 등) 미구현** — 지금은 오로라 애니메이션 프리뷰만 있음.
6. QA에 "대화 교류감/후속 질문 여부"를 직접 채점하는 기준은 아직 없음 (`warmth_persona`가
   느슨하게 겹치지만 전용 기준은 아님) — 창업자가 "QA가 소통 부분을 안 본다"고 지적한 부분,
   다음에 전용 기준으로 만들지 결정 필요.

## 다음 후보 작업 (우선순위 미정 — 창업자 결정 필요)

| 항목 | 버킷 | 비고 |
|---|---|---|
| ~~A. 피드백 질문 문구 다양화~~ | 완료(제거로 대체) | 다양화 대신 기능 자체를 삭제하기로 결정됨 |
| B. 매일 아침 프로액티브 오프닝 실제 구현 | ⚠️ 제약 있음 | 서버 크론(Vercel Cron Jobs) + 알림 발송 경로 필요, 구조 변경 수반 |
| C. 실제 카메라 캡처 연동 (getUserMedia) | ⚠️ 제약 있음 | 브라우저 권한/HTTPS 필요, 사진 기반 질문 흐름 설계 필요 |
| D. 음성 대화 (Gemini Live 또는 Web Speech API) | ⚠️ 제약 있음 | 별도 API·비용, 브라우저 호환성 검토 필요 |
| E. QA 스위트 실제 API 실행 | ✅ 지금 가능 (Gemini 기본이라 사실상 무료) | 약 90~100회 호출, Gemini 무료 티어 한도만 유의 |
| F. 크리에이티브 변경사항 브라우저 확인 | ⚠️ 제약 있음 | 이 환경에 브라우저 도구 없음 — 창업자가 직접 열어보고 피드백 필요 |
| G. 지도/경제 등 실시간 데이터 정확도 개선 (기존 ⑤) | ⚠️ 제약 있음 | 공공 API·시세 API 연동 필요, 추후 로드맵과 연결 |
| H. 나머지 6개 주제(검색/지도/사주/경제/말벗/부동산) QA 시나리오 커버리지 확대 | ✅ 지금 가능 | 지금은 정치/부동산 중립성 1개 시나리오만 있음, 나머지는 미검증 |
| I. Pretendard 폰트 실제 로드 (CDN/@font-face) | ✅ 지금 가능, 작고 빠름 | 크리에이티브 리뷰 ②번, 타이포 정체성 부재 해결 |
| J. 오로라 보더 색상을 앱 토큰으로 교체 | ✅ 지금 가능 | 크리에이티브 리뷰 ③번 |
| K. `#EEF2FF`+navy를 `--accent-soft` 역할 변수로 토큰화 | ✅ 지금 가능 | 크리에이티브 리뷰 ④번 |
| L. 카드 depth(그림자) 일관성 정리 | ✅ 지금 가능, 다소 손이 감 | 크리에이티브 리뷰 ⑤번 |
| M. 타입 스케일을 5~6단계 비율로 정리 | ⚠️ 손이 많이 감 | 크리에이티브 리뷰 ⑥번, 전 화면 폰트 사이즈 재검토 필요 |
| N. 스플래시 화면(브랜드 모먼트) 추가 | ✅ 지금 가능 | 크리에이티브 리뷰 ⑦번 |
| O. "대화 교류감/후속 질문 여부" 전용 QA 기준 추가 | ✅ 지금 가능 | 창업자가 "QA가 소통 부분을 안 본다"고 지적, `warmth_persona`와 별도로 만들지 결정 필요 |
| P. `--blue`/`--navy` 변수를 `--accent`/`--accent-strong`으로 이름까지 정리 | ✅ 지금 가능, 작음 | 지금은 값만 베이지로 바뀌고 이름은 그대로라 혼동 소지 |
| Q. 카나나 리디자인 실제 화면 확인 | ⚠️ 브라우저 도구 없음 | 색상 대비/링 아이콘/피드백 아이콘 배치 검증 필요 |

## 추후 로드맵 (사업계획서 근거, 지금 단계 아님)

- 공공 API 연동 (버스 시간 등, 슬라이드 12)
- 웹 CRM / B2B·B2G 대시보드 (슬라이드 8, 13)
- 서버 DB 연동 (Supabase/Firebase) — 기기 간 동기화, 다중 사용자 피드백 통합, 로그인/계정
- 4단계 과금 체계 실제 결제 연동 (슬라이드 13)
